Clean Up Workspaces
Find Large Workspaces:
  du -sh /var/lib/jenkins/workspace/* | sort -h

Delete Workspaces for Completed Jobs: Remove workspace folders manually for jobs that don’t need them:
  rm -rf /var/lib/jenkins/workspace/<job-name>

GIT Commands
git add . or git add <filename>  #stage changes

git commit -m "message" <filename>  #commit file

git push origin main  #push it to remote

git pull origin main     #to pull changes from remote branch

git branch  # view all branches

git checkout <beranch name> # to switch branch

git status  # view staged files
